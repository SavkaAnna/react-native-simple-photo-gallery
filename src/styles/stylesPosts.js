import { StyleSheet } from 'react-native';

const stylePosts = StyleSheet.create({
    post: {
        flexDirection: "row",
        flexWrap: "wrap",
        justifyContent: "space-between",
        padding: 20,
        overflow: 'scroll'
    }
});

export default stylePosts;